{
  "name": "thumbnail-generator",
  "version": "1.0.0",
  "description": "Serverless thumbnail generator using Sharp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=20.x"
  }
}
